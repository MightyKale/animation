/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  // basePath: '/metaverse', 
  // assetPrefix: '/metaverse/', 
};

module.exports = nextConfig;
